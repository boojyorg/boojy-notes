{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "compactPrompt": "When compacting this development session, you MUST preserve verbatim: the Beta section of docs/BACKLOG.md, the architecture rules and conventions from AGENTS.md and .claude/rules/, the explicit file paths modified during this branch, and the precise error signatures currently under evaluation. Discard historical conversational prose, greeting filler, and resolved styling discussions.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "./.claude/hooks/post-edit-validation.sh"
          }
        ]
      }
    ]
  }
}
