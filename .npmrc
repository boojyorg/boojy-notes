# pnpm: use a flat (hoisted) node_modules so electron-builder and the
# node-pty native rebuild resolve dependencies the same way npm did.
# Required for `electron-builder` + `npmRebuild: true` to package node-pty.
node-linker=hoisted
